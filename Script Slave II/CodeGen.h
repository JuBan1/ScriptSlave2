#ifndef CODEGEN_H
#define CODEGEN_H

#include <vector>
#include <string>
#include "ASTNode.h"
#include "SymbolScope.h"
/*
class CodeGen
{
public:

	bool Generate( StartBlockPtr& node, const SymbolScope& table );
	bool WriteToFile( std::string fileName );

private:
	std::vector<std::string> m_code;
};
*/
#endif